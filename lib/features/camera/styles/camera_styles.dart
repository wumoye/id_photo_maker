import 'package:flutter/material.dart';

class CameraStyles {
  static const appBarStyle = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
  );

  static final cameraPreviewStyle = BoxDecoration(
    color: Colors.black,
    border: Border.all(color: Colors.transparent),
  );
}